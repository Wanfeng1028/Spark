/**
 * 错误码→人话文案表已下沉 @spark/protocol（工单 8.1 / D22 四端共享资产）：
 * 单一来源在 packages/protocol/src/error-copy.ts；本文件保留 web 内 `@/lib/error-copy`
 * 导入面的再导出。
 */
export { ERROR_COPY, humanizeError, errorMessageOf } from '@spark/protocol'
export type { ErrorCopy } from '@spark/protocol'

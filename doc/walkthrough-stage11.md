# 阶段十一走查记录——验收尾巴清账（工单 11.2）

> 执行主体：**晚风（用户现场执行）**；本文件是记录模板与执行清单（AI 侧准备件，工单 11.2）。
> 走查矩阵口径来自 [doc/06 §5](./06-testing-plan.md) 四幕模板（正常 / 审批 / 中断 / 断线重连）；
> 现场发现的缺陷**先记录后开单**（缺陷修复不等同于本工单验收）。
> 记录格式：每格填 `版本（commit 前 7 位）/ 日期 / 结果（PASS·FAIL·BLOCK）/ 证据（截图或录屏相对路径）`；
> 证据统一放 `doc/walkthrough-assets/`（不入库大文件时放网络盘并在表格注明链接）。

## 版本记录

| 版本 | 日期       | 作者                                                                                                                                                                | 变更内容                                       |
| ---- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| v1.0 | 2026-09-02 | AI 编写：ZCode CLI · GLM-5.3-Flash（`builtin:zai-start-plan/GLM-5.3-Flash`）；发起：晚风（Wanfeng1028，阶段十一 11.2 指令） | 初稿：四幕×五端走查矩阵模板 + 用户执行清单 8 项 |

## 一、四幕 × 五端走查矩阵

| 幕＼端 | web | desktop（Electron） | cli（Ink TUI） | mobile（Expo） | miniapp（Taro） |
| ------ | --- | ------------------- | -------------- | -------------- | --------------- |
| ① 正常回合（建会话→发消息→流式→定稿） | / | / | / | / | / |
| ② 审批（触发 bash 写类工具→四选项通过一次；再触发一次拒绝） | / | / | / | / | / |
| ③ 中断（流式中停止 ■ / esc；回合 aborted 闭合） | / | / | / | / | / |
| ④ 断线重连（杀 server→重连→durable 重放无丢失无重复） | / | / | / | / | / |

- desktop 行复用 web 结果的部分（同一渲染层）可注"同 web + 壳特有项"（托盘/通知/NSIS 安装见清单第 4 项）。
- mobile/miniapp 走查前置：配对鉴权流程（扫码→6 位码→长效 token）先通，再进四幕。

## 二、用户执行清单（按依赖顺序 8 项）

| #  | 项目                               | 前置条件                                             | 操作                                                                                                   | 预期产物（证据）                                       |
| -- | ---------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------ |
| 1  | G1-A 真实模型闭环                  | `export DEEPSEEK_API_KEY=sk-xxx`；bash 环境          | `bash examples/e2e-smoke.sh`（场景 A 段）                                                              | 终端输出归档 `walkthrough-assets/e2e-A.log`            |
| 2  | G1-B SSE 断线重连                  | 无（不需要 key）                                     | 同上（场景 B 段——11.2 已修 since 口径，测真实水位语义）                                                | `e2e-B.log`                                            |
| 3  | G1-C kill -9 崩溃恢复              | 无                                                   | 同上（场景 C 段——半写截断 + SIGKILL + 重启补闭合）                                                     | `e2e-C.log`                                            |
| 4  | Windows 安装走查（NSIS）           | Windows 主机                                         | gh 手动触发 desktop-win workflow → 下载 artifact → 安装 → 桌面壳四能力（起 server/连接 web/托盘/退出回收） | 安装录屏 + 安装后截图                                  |
| 5  | 沙箱隔离验证                       | 同上主机                                             | 设置页沙箱档开 → 会话内 bash 写 `$HOME` 外路径 → 预期被拒（对照 ADR D15 语义）                          | 会话截图（拒绝文案）                                   |
| 6  | 真实外部 MCP server 演示           | npx 可用                                             | `~/.spark/mcp.json` 加 `@modelcontextprotocol/server-filesystem` → 会话内调用其工具 → 回合正常闭合      | 会话截图（工具块与结果）                               |
| 7  | 真实模型子代理演示                 | DEEPSEEK_API_KEY                                     | 会话内派发子代理任务 → 子代理回合完成 → 主会话汇总                                                      | 会话截图（子代理块）                                   |
| 8  | 移动端真机/模拟器四幕 + 小程序走查 | 真机或模拟器 + 桌面端已起 server；微信开发者工具     | 按本表矩阵逐格执行（mobile/miniapp 两列）                                                               | 矩阵表格填毕 + 关键格截图                              |

## 三、结果汇总（执行后填）

- 通过项：__ / 8；缺陷清单（每条开修复工单编号）：__
- 全部通过后：doc/05 G1 行注记"已消解（日期，11.2）"，本表归档。

{
  "name": "@spark/example-bot",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "SDK 画廊·automation bot（工单 14.5）：建会话 → 发任务 → 等 turn.completed → 写日志；SPARK_DEMO=1 走进程内通道离线演示",
  "scripts": {
    "start": "tsx bot.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@spark/protocol": "workspace:*",
    "@spark/sdk": "workspace:*"
  },
  "devDependencies": {
    "@spark/engine": "workspace:*",
    "@types/node": "^24",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "contributors": [
    "Jules (AI Assistant)",
    "Doubao (AI Assistant)",
    "opencode (AI Assistant)"
  ]
}

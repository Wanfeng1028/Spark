# dependencies
node_modules/
.pnpm-store/

# Expo 产物（工单 9.2 评审 G8；prebuild 原生目录 android/ 与 ios/ 待工单 9.5 决策口径）
.expo/

# build outputs
dist/
build/
out/
release/
*.tsbuildinfo

# env & secrets
.env
.env.*
!.env.example

# logs & runtime
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# test & coverage
coverage/
.vitest/

# SWC 插件缓存（Taro/swc 构建运行时产物，工单 9.4）
.swc/

# Playwright 运行产物（trace/失败上下文；__screenshots__ 基线截图入库）
apps/web/e2e/.artifacts/
apps/web/playwright/.cache/
# 本地工具产物（HTML share 预览等），不入库
.trae-html-share-packages/
.qoder/better-harness/

# IDE & OS
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.DS_Store
Thumbs.db

# spark runtime data（本地引擎产物，不入库）
.spark/
~/.spark/
tool-outputs/

# mock 录制的大文件按需排除
# examples/mock-sessions/*.large.json

# 临时产物落地区：各 AI 会话的调试输出统一丢这里，最终由人类整目录删除
# （单列 + 忽略——防多会话并行时 git add -A 把临时产物扫进提交）
_scratch/

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
bower_components/
jspm_packages/
__pycache__/
site-packages/
venv/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）

# DESIGN.md — Spark 视觉与交互设计规则

## 版本记录

| 版本 | 日期       | 作者                                                                                                                                                                                               | 变更内容                                                                                                                                                                                                                                                                                                                   |
| ---- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v1.0 | 2026-08-22 | AI 编写：ZCode CLI · GLM-5.3（`builtin:zai-start-plan/GLM-5.3`；会话内部标识 ox-alpha，model id `57d26d76-3d24-4c1c-95b3-88fcc03173f9/stealth/ox-alpha`）；人作者：晚风（Wanfeng1028，发起与审核） | 初稿（时为 doc/04-frontend-rules.md）：目标定义（对标 Codex/ZCode 桌面端）、布局/密度排版/颜色/交互/动效规范、反网站化黑名单、AI Elements 桌面化改造规范、组件 DoD、Electron 预留                                                                                                                                          |
| v1.1 | 2026-08-23 | AI 编写：ZCode CLI · GLM-5.3（`builtin:zai-start-plan/GLM-5.3`）；决策：晚风（Wanfeng1028）                                                                                                        | **由 doc/04-frontend-rules.md 迁移为根 DESIGN.md**（按四类约束框架：视觉规则归 DESIGN.md，单一来源）；"AI 生成风"判例引用改指 ARCHITECTURE.md D2                                                                                                                                                                           |
| v1.2 | 2026-08-23 | AI 编写：ZCode CLI · GLM-5.3（`builtin:zai-start-plan/GLM-5.3`）；发起：晚风（Wanfeng1028）                                                                                                        | 新增 **§12"AI 生成风"黑名单完整版**（六类：颜色/玻璃特效/字体排版/卡片边框/布局/图像动效；依据外部调研 5 源，附成因考证与来源链接）；§4 禁止条目收窄为配色项并指向 §12；补"暗色默认"辨析                                                                                                                                   |
| v1.3 | 2026-08-23 | AI 编写：ZCode CLI · GLM-5.3（`builtin:zai-start-plan/GLM-5.3`）；发起：晚风（Wanfeng1028，"继续去找，确保开发后不出现 AI 前端感觉"）                                                              | §12 深化：每条加注严重度分级 P0/P1/P2（依 avoid-ai-design）；新增 **§12.7 文案与语气**（Elevate 式空泛词/通用 CTA/箭头按钮/filler microcopy）与 **§12.8 代码级硬检查**（11 项 grep 黑名单，阶段一接 CI）；调研来源扩至 6 项（+avoid-ai-design 全库/Hallmark 65 门禁细节）；交叉引用补 ARCHITECTURE.md §9（后端 AI 味清单） |
| v2.0 | 2026-08-26 | AI 编写：ZCode CLI · ox-alpha（model id `57d26d76-3d24-4c1c-95b3-88fcc03173f9/stealth/ox-alpha`）；发起与供图：晚风（Wanfeng1028，D3 视觉规格指令 + ZCode 2026-08 版全量截图 23 张）               | **新增 §13 ZCode 化视觉规格**（阶段六~九实现依据，参考 ZCode 2026-08 版，主布局援引 Codex app 公开资料旁证）：界面三分类总纲、布局栅格（§13.A）、控件规格（§13.B）、**亮色默认**全量 token 表（§13.C）、设置中心三组 15 页逐字段规格（§13.D）、Composer 与权限四档（§13.E）、管理页四节（§13.F）、聚合投影页（§13.G）、会话域实测形态（§13.H）、反 AI 味四端条款（§13.I）；§1 主题基调翻转、§2/§3/§4 旧数值标注由 §13 接管 |
| v2.1 | 2026-08-26 | 同上（勘误：晚风侧 doc/02 v3.0 编制会话核对发现）                                                                                                                                                  | §13 口径行勘误：实现工单指针 "doc/02 §9 阶段六"→"**§8 阶段六**"——doc/02 v3.0 将阶段六~九写入 §8（§9 为参考速查表，被 check_doc_links.py 事实锚点占用，路线图不可占用该编号）                                                                                                                                 |
| v2.2 | 2026-08-26 | AI 编写：ZCode CLI · ox-alpha（model id `57d26d76-3d24-4c1c-95b3-88fcc03173f9/stealth/ox-alpha`）；发起与供图：晚风（Wanfeng1028，移动端规格指令 + Qoder CN iOS v1.32.0 实拍 13 张）               | **新增 §13.J 移动端规格**（iOS/Android App，工单 9.2/9.3 实现依据；框架=Expo+RN 经用户确认，D20 维持）：J.0 设计基调（浅灰底白卡无边框分层/accent 唯一不引入参考品绿）/J.1 导航（无 tab bar 单栈+抽屉）/J.2 逐页 11 页实测映射（首页空态/会话列表/会话页投影迁移/设置/用量/通知/清理缓存/账号与安全/扫码配对为主手输兜底/配对引导线框卡/归档抽屉）/J.3 控件规格表/J.4 空态加载错误/J.5 反 AI 味补充；尾注版本号同步 v2.2 |
| v2.3 | 2026-08-26 | 同上（补供图：Qoder CN 会话页有内容态 2 张）                                                                                                                                                       | **§13.J.3 会话页升级为实测规格**（此前"未截图按 §13.H 迁移"作废）：user 消息=右对齐浅灰胶囊（radius 18、最大宽 80%、无头像——移动端与 web 全宽块的唯一形态分歧）/assistant=全宽纯文本/assistant 尾操作行（复制+👍👎+“内容由 AI 生成”合规标注，👍👎 记 v2）/居中时间戳分隔（>30min）/回到底部浮动钮/页头副标题（项目名）与会话菜单（置顶/重命名/反馈/归档/删除红字）；删除/归档暴露后端缺口 → doc/02 §8.7 新增 V2-23 |
| v2.4 | 2026-08-30 | AI 编写：Qoder；发起：晚风（Wanfeng1028，阶段九开工指令）                                                                                                                            | 移动端落地收敛记录（工单 9.2 三维评审 G9）：§13.J.1 补抽屉入口收敛说明（抽屉=全部会话/设置两项：配对入口并入设置页、项目分组随 v2 项目域）；§13.C `--sidebar-bg`（亮 #fafafa/暗 #18181b）与侧栏选中项（底 #f4f4f5 + 1px #e4e4e7 边）移动端抽屉同口径映射 |

> 本文件是**视觉决策文档**：回答"页面应该保持什么风格，遇到新场景怎么选"，让不同页面看起来仍属于同一个产品。
> 架构与设计决策见 `ARCHITECTURE.md`；实现规格（做什么）见 `doc/02-development-plan.md` §6——本文件管"做成什么感觉、什么不许做"。所有前端 PR 以本文为验收依据之一。

---

## 1. 目标定义：什么叫"Codex/ZCode 桌面的感觉"

对标 Codex 桌面 app（Electron）与 ZCode（ADE）的共性，提炼六个关键词：

| 关键词             | 含义                                                            | 反例（禁止的样子）             |
| ------------------ | --------------------------------------------------------------- | ------------------------------ |
| **开发者工具气质** | 高信息密度、克制配色、mono 字体运用、类 VS Code/Linear 的冷静感 | 营销页配色、大圆角卡片堆叠     |
| **转录式会话流**   | 会话是"执行记录/控制台日志"：全宽块 + 小角色标签 + 可折叠工具块 | ChatGPT 网页式大头像气泡聊天   |
| **固定工作台**     | 侧栏+主区+状态条的 AppShell，面板可折叠，无页面跳转             | 多级路由页面、面包屑导航       |
| **键盘优先**       | 全局命令面板、快捷键体系、Esc/Enter 语义清晰                    | 只能鼠标点、hover 才能发现功能 |
| **即时反馈**       | 一切异步有状态指示（连接点/运行徽标/进度），无假状态            | 点了没反应、loading 转圈半天   |
| **亮色默认**       | v2.0 起 light 为默认主题（§13.C，工单 6.1）；dark 与"跟随系统"都是一级公民 | 只做深色、亮色是补丁         |

**视觉基准**：Codex 桌面 app 截图、ZCode ADE、Claude Code TUI 的内容块行为、Linear/VS Code 的密度与克制。

## 2. 布局规范

```
┌─ titlebar 占位 36px（现显示应用名；Electron 期换自定义标题栏+窗口控制）─┐
┌──────────┬────────────────────────────────────────────┐
│ Sidebar  │ 主区（ChatView 或 阶段四: 会话流+工作区双栏）  │
│ 240px    │                                            │
│ (200-360 │  只有此区域纵向滚动                           │
│  可拖拽,  │                                            │
│  可折叠48)│                                            │
├──────────┴────────────────────────────────────────────┤
│ StatusBar 28px：连接● 模型名 seq 水位 token 累计 主题切换 │
└───────────────────────────────────────────────────────┘
```

- **最小窗口 1280×800**；不做移动端、不做响应式断点（这是桌面应用，不是网站——窗口过小出滚动条即可）。
- **页面级禁止滚动**：html/body 锁 100%（flex/grid 满屏容器）；只有 ChatView 内容区与 Sidebar 列表内部滚动。
- 主区双栏预留：Grid 布局预留 `conversation | workspace` 两列（阶段四接 diff/文件树），先单列占满。
- 浮层（Dialog/Popover/ContextMenu）用 Radix portal，不挤占布局。
- **v2.0 起布局数值由 §13.A 接管**（左栏 264px 可折叠、主区内容列 max-width 768px、StatusBar 单行 24px、右栏 360px）；本节 240px/28px 为阶段一~五现状数值。

## 3. 密度与排版

| 项                 | 规则                                                        |
| ------------------ | ----------------------------------------------------------- |
| 基础字号           | UI **13px**；辅助信息 12px；页面级标题 15px（最大不超过）   |
| 代码/路径/工具输出 | **一律 mono**（IBM Plex Mono 12.5px），UI 用系统栈          |
| 行高               | 正文 1.5；列表 1.3（紧凑）                                  |
| 间距               | **4/8px 网格**；块内边距 8-12px；区块间距 16px（不超过 24） |
| 列表行高           | 32px（紧凑模式 28px）；Sidebar 会话项 36px                  |
| 图标               | lucide 16px（状态点 8px 圆）                                |
| 按钮               | 高 28-32px（紧凑 24px）；主按钮每屏至多一个                 |
| 圆角               | 控件 6px；卡片 8px；**最大不超过 12px**                     |
| 边框               | 1px `--border`；分隔优先用边框与留白，不用阴影              |

**会话流形态（转录式，硬约束）**：消息全宽纵向排列；角色用 12px 灰色小标签（`YOU` / 模型名），**不用大头像、不用大圆角对话气泡**；用户消息用浅色背景块（4px 圆角、全宽）与助手消息区分；工具调用是独立可折叠块（见 §8）。

## 4. 颜色规范（token 定义）

亮色默认（v2.0 翻转，工单 6.1；全量 token 与 AA 标注见 §13.C，下表保留两主题速查）。中性基调 = shadcn zinc 系；**单一 accent** + 三个语义色，全部走 CSS variables（styles/tokens.css），**禁止组件里出现硬编码 hex**。

| Token                        | 用途               | dark 建议值                        | light 建议值        |
| ---------------------------- | ------------------ | ---------------------------------- | ------------------- |
| `--background/--foreground`  | 基底               | `#09090b / #fafafa`                | `#ffffff / #18181b` |
| `--muted/--muted-foreground` | 次要信息           | `#18181b / #a1a1aa`                | `#f4f4f5 / #71717a` |
| `--border`                   | 边框/分隔          | `#27272a`                          | `#e4e4e7`           |
| `--primary`                  | 主按钮/焦点        | `#fafafa`（白按钮黑字，Linear 式） | `#18181b`           |
| `--spark-accent`             | 运行中/链接/焦点环 | `#818cf8`（indigo-400）            | `#4f46e5`           |
| `--spark-warn`               | 审批挂起/警告      | `#fbbf24`（amber-400）             | `#b45309`           |
| `--spark-ok`                 | 成功/完成          | `#34d399`                          | `#047857`           |
| `--spark-err`                | 错误/危险          | `#f87171`                          | `#b91c1c`           |

- accent 只用于：运行中指示、焦点环、链接、选中态——**不做大面积强调**。
- 语义色只表达状态，不做装饰。
- 文本对比度 ≥ 4.5:1（两主题都查）。
- **绝对禁止"AI 生成风"配色**（完整六类黑名单见 §12，判例与决策记录见 ARCHITECTURE.md D2）：蓝紫渐变玻璃拟态；**暖棕/米色等暖调配色**；渐变文字/渐变数字；大面积彩色块；发光边框；多层阴影（浮层至多一层 subtle shadow）。

## 5. 交互规范

- **键盘优先**：`Cmd/Ctrl+K` 全局命令面板（切换会话/新建/操作）；`Enter` 发送、`Shift+Enter` 换行、`Ctrl+Enter` 强制 queue、`Esc` 关闭浮层/取消编辑、`Cmd/Ctrl+,` 打开设置。快捷键提示统一写在 Tooltip 与菜单右侧（`⌘K` 样式）。
- **禁用原生 `alert/confirm/prompt`**：一律自绘 Dialog 或内联确认。
- **右键上下文菜单**：会话列表项（重命名/删除）、工具块（复制输出）。
- **危险操作内联确认**：删除按钮点击后变红"确认删除？"（3s 超时还原），不弹窗。
- Tooltip 统一 300ms 延迟出现、即时消失。
- 焦点管理：所有可交互元素 `:focus-visible` 2px accent ring；浮层关闭后焦点归还触发元素。
- **一切异步动作必须有反馈**：按钮点击 ≤150ms 出现 loading 态；提交三态（started/steered/queued）以 toast/内联提示告知。
- **禁止假状态**：UI 状态只能来自事件流（applyEvent）；mock 场景也必须走事件，不允许组件写死演示数据。

## 6. 动效规范

- 只允许**微动效**：opacity/transform，120-160ms ease-out；列表项增删 150ms 淡入淡出。
- 流式光标：`▮` 1s steps(2) 闪烁；审批 resolved 徽标显示 2s 后收起。
- **禁止**：页面/视图转场动画、大位移动画、视差、无限循环动画（loading spinner 除外）、入场编排动画。
- `prefers-reduced-motion: reduce` 时全部动效禁用（保留瞬时状态切换）。

## 7. 反"网站化"黑名单（硬约束，PR 审查逐条对照）

1. 禁止 hero 区/大标题/落地页式空态——空态是紧凑引导块（一段话+主按钮+提示词 chips）。
2. 禁止营销组件：轮播、marquee、大 CTA 按钮、emoji 主视觉、客户 logo 墙。
3. 禁止整页滚动、scroll-snap、锚点导航。
4. 禁止外部链接在应用内打开（一律 `target="_blank"` 交给系统浏览器）。
5. **禁止请求任何外部资源**：字体/图标全本地打包（@fontsource），不引 CDN、不引谷歌字体、无埋点分析。
6. 禁止登录页/cookie banner/营销 footer/版本更新弹窗营销文案。
7. 禁止大头像气泡聊天形态（见 §3 转录式约束）。
8. 禁止乐观更新/本地先行状态（一致性来自 durable 回放，见 doc/02 §6.4）。

## 8. 组件桌面化改造规范（AI Elements copy-in 之后必做）

| 组件                  | 桌面化规则                                                                                                                                            |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 全部 copy-in 组件     | 密度改造对齐 §3（13px/28-32px 高/8px 内边距/6px 圆角）；样式只改 token 与 Tailwind 类                                                                 |
| **ToolCard**          | 默认**折叠为一行摘要**：`[图标] 工具名 · 资源路径 · 状态 · 耗时`；点击展开详情；running 时摘要行显示微型 spinner+最后输出片段；错误态红字摘要默认展开 |
| Terminal（bash 输出） | mono 12px；自动滚底（用户上滚则暂停+BackBottom）；缓冲上限 500 行（超出截头）；右上角复制按钮                                                         |
| Diff（edit/write）    | unified diff：增行低饱和绿底、删行低饱和红底；文件名栏 mono；hunk 头灰底                                                                              |
| **ApprovalCard**      | `--spark-warn` 左边框 3px + 浅 warn 背景；三按钮：主=[允许一次]、[总是允许]、[拒绝]（拒绝展开 feedback 输入）；resolved 后徽标 2s 收起为摘要行        |
| Reasoning             | 折叠面板，摘要行显示首行+token 数；流式时自动展开、结束自动折叠（手动操作优先）                                                                       |
| Composer              | 三模式（空闲/进行中/审批禁用，见 doc/02 §6.2）；进行中时 [插话][排队][停止] 三个次要按钮+主按钮变 [插话]                                              |
| 状态点                | 8px 圆：绿=空闲、accent 脉动=运行中、amber=等待审批、红=断线                                                                                          |

## 9. 状态与数据（补充约束）

- 组件**不得直接 fetch**——一切数据经 store selector（违反即架构破坏）。
- 全局连接状态必须在 StatusBar 常显 + 断线时顶部条强提示（回放进度细条）。
- 每个视图/组件的空态、加载态、错误态是**完成定义的一部分**，不允许"以后补"。

## 10. 组件完成定义（DoD，PR 自查清单）

- [ ] props 全类型无 `any`；受控/非受控语义明确
- [ ] 空/加载/错误/禁用 四态齐备
- [ ] 键盘可达 + `:focus-visible` + 合理 Tab 序
- [ ] 深浅两主题下人工检查（对比度 ≥4.5:1）
- [ ] 1280×800 与 1920×1080 两档窗口检查
- [ ] `prefers-reduced-motion` 下检查
- [ ] 消费新事件类型的：applyEvent 单测已加
- [ ] 无外部资源引用（grep 检查 cdn/http 链接）
- [ ] 密度符合 §3（字号/行高/内边距/圆角）

## 11. Electron 桌面化预留（现在就埋，阶段五免返工）

- AppShell 顶部 36px titlebar 占位区（现在放应用名+主题切换，Electron 期替换为自定义标题栏 + 窗口控制按钮）。
- 不用 `window.open`；外部链接 `target="_blank"`。
- 布局不依赖 body 滚动（满屏 flex/grid），`100vh` 用 `100dvh` 兜底。
- 字体/图标本地打包（§7.5）保证离线可用。
- window-state 持久化、系统菜单、托盘：阶段五再接。

## 12. "AI 生成风"黑名单（完整清单，硬约束——PR 审查逐条对照）

> 本节是全仓库"AI 生成风"特征的**唯一完整清单**（AGENTS.md §2.6 / ARCHITECTURE.md D2 / 工具 shim 均引用此处，不复制）。与 §7 反"网站化"黑名单互补：§7 挡"营销网站味"，本节挡"AI 模板味"；后端/通用代码的"AI 生成味"黑名单在 ARCHITECTURE.md §9。
> **成因**（2026-08-23 外部调研考证）：主流模型的训练语料被 v0/Lovable 时代模板与 Tailwind 早期默认审美污染——Tailwind 早期文档所有按钮示例默认 `indigo-500`，作者 Adam Wathan 已公开致歉；"做界面"于是被学成了"套默认模板"。
> 辨析：社区另把"默认永久暗色"列为 AI 特征——那是针对营销落地页的；本产品 v2.0 起**亮色为默认主题**（§13.C，工单 6.1），深/浅/跟随系统三档并存且都是一级公民，与此无关。
> **严重度分级**（依社区审计工具 avoid-ai-design 的分级法）：**P0 一眼 AI**——出现即打回，无需讨论；**P1 强信号**——默认禁止，除非 PR 中给出书面理由；**P2 细微信号**——评审时说明为什么。

### 12.1 颜色

- **蓝紫渐变**（紫→蓝、indigo/violet 系渐变按钮/背景/标题文字）——出现频率最高的 AI 特征。【P0】
- "vibecode 紫"：淡紫/薰衣草色作主色调。【P0】
- Tailwind 默认蓝（`blue-600` 主按钮/链接）+ shadcn 默认 zinc/slate 的"没调过"配色。【P0】
- 暖棕/米色等暖调配色（判例：2026-08-22 评审的刷课工具截图）。【P0】
- 渐变文字（`text-transparent bg-clip-text` + 渐变）、渐变大数字。【P0】
- 渐变光球（purple orb）/ mesh 渐变漂浮在内容区后面。【P0】

### 12.2 玻璃与特效

- 毛玻璃/backdrop-blur 用在按钮、卡片、导航栏（glassmorphism-on-everything，社区称"reflexive glassmorphism"）。【P0】
- **实线细描边 + 内部模糊**的组合按钮/卡片（判例特征）。【P0】
- 发光边框（glowing border）、暗色霓虹（neon cyan/violet）、accent-glow 动画光晕。【P1】
- 多层阴影堆叠、装饰性大阴影（shadcn 默认卡 `rounded-2xl`+`shadow-lg` 原样出现）。【P1】

### 12.3 字体与排版

- **超大标题字体**（hero 式 text-4xl/5xl/6xl/7xl 粗体大标题）——§3 封顶：UI 13px、页面级标题 15px。【P0】
- Inter / Poppins / Space Grotesk / Geist 等被当成"不用想"的默认主字体（Hallmark 称 Inter "在训练数据里被过度代表"）。【P1——我们用系统栈 + IBM Plex Mono】
- 全大写 section 小标签滥用；正文中孤立的衬线斜体"强调词"。【P1】
- 装饰性等宽字体（"hacker vibe"）——mono 只用于代码/路径/工具输出（§3）。【P1】
- 全篇一档字号一字重的"扁平"层级；拉大 letter-spacing 当"设计感"。【P2】

### 12.4 卡片与边框

- "卡片灾难"（cardocalypse）：每个块都包卡片、卡片套卡片、用嵌套卡片表达层级（Hallmark 模式库编号 #18）。【P0】
- 千篇一律的模板圆角（所有卡片一律 16px/`rounded-2xl`/`rounded-3xl`）——§3 圆角封顶：控件 6px、卡片 8px、最大 12px。【P1】
- 每张卡片同一条灰色 1px 平边（flat gray border on every card）。【P1】
- 纯装饰的彩色 3-4px 左边框条（社区称"最可靠的 AI 味信号"）——左边框只允许表达语义状态（如 §8 ApprovalCard 的 warn 左边框）。【P1】
- "icon-in-a-rounded-square"：小图标装进圆角色块当列表符号/装饰单元。【P1】

### 12.5 布局

- 居中 hero + 徽章 pill + 大标题 + CTA 的落地页骨架（应用内无落地页，§7.1）。【P0】
- 恰好三张特性卡一行、"1·2·3"编号步骤行、横向统计数字条。【P1】
- bento 网格默认化（信息本可线性呈现却硬切成大小格子）。【P1】
- 假仪表盘/装饰性统计卡（与真实数据无关的 mock 数字块——同时违反 §5"禁止假状态"）。【P0】
- 全页统一 `gap-4`/`p-6`、无层级差的均匀间距（.spacing 没有节奏）。【P2】

### 12.6 图像与动效

- 3D 抽象漂浮物、塑料质感插画、通用库存图（"多样化团队看笔记本"）。【P1】
- 标题上方居中的巨大圆角图标（Lucide 放大到 48px+ 当主视觉）。【P1】
- **emoji 当装饰、功能图标或列表符号**（✨🚀⚡，AI 刷课工具截图的星号装饰即判例同款）——中文社区总结的"AI 四大通病"之一。【P1】
- 所有元素同一个 fade-in 入场编排、按钮 hover 无过渡或瞬移、动效完全为零——动效规则见 §6（只允许微动效）。【P2】

### 12.7 文案与语气（UI copy）

- "Elevate / Seamless / Powerful / Effortless"式空泛形容词（中文对应："强大""优雅""轻松驾驭"当标题修饰语）。【P1】
- 通用 CTA 与模板文案："Get Started/开始使用"按钮、营销 footer、四列 footer。【P1】
- 按钮文案末尾焊死一个箭头符号（"开始 →"）。【P1】
- filler microcopy：功能卡下那句"Beautiful dashboards to visualize your data"式的说明废话。【P1】
- 营销腔：感叹号、"Revolutionize/彻底改变你的工作流"式口号、对标语的押韵感。【P1】
- 文案语言与产品语言不一致（界面中文却冒英文口号，反之亦然）。【P2】

### 12.8 代码级硬检查（grep 清单——阶段一接入 CI 脚本；当前每次前端 PR 人工 grep 自查）

在 `apps/web`、`components/ui` 范围内，以下模式**零命中**（确需白名单例外的，PR 中逐条写明理由）：

| grep 模式                                                       | 对应禁止项                                     |
| --------------------------------------------------------------- | ---------------------------------------------- |
| `bg-gradient-to` / `bg-linear-`                                 | 渐变背景（12.1）                               |
| `from-purple` `from-indigo` `from-violet` `to-blue-`            | 蓝紫渐变（12.1）                               |
| `bg-clip-text`                                                  | 渐变文字（12.1）                               |
| `backdrop-blur`                                                 | 毛玻璃（12.2）                                 |
| `rounded-2xl` `rounded-3xl`                                     | 模板大圆角（12.4）                             |
| `shadow-lg` `shadow-2xl`                                        | 装饰性大阴影（12.2/12.4）                      |
| `text-4xl` `text-5xl` `text-6xl` `text-7xl`                     | 超大标题（12.3）                               |
| `animate-pulse` `animate-bounce`                                | 无意义循环动效（§6；spinner/状态点白名单除外） |
| `Inter` `Poppins` `Space Grotesk` `Geist`                       | 默认主字体（12.3）                             |
| `fonts.googleapis` `cdn.` `http://` `https://`（字体/图标引用） | 外部资源（§7.5；API 端点除外）                 |
| ✨ 🚀 ⚡ 🎉 等 emoji                                            | emoji 装饰（12.6/12.7）                        |

配套纪律：**软指令与硬检查分开**（AGENTS.md §8）——本节 markdown 是提醒层，上表 grep + 后续 CI 是强制层；组件改造流程（frontend-component SKILL）第 4 步已挂接本清单。

**正面表述**：我们是 13px 密度、zinc 中性基调、单一 accent、转录式会话流的开发者工作台（§1-§6 已定义）；凡"看起来像 v0/Lovable 直出"的界面一律打回。社区反向建议中可取者已吸收进本文件：调色板 ≤3 色相（§4 token）、真实字体配对（我们用系统栈 + IBM Plex Mono）、8pt 间距网格（§3）、边框只做分隔不做装饰（§3）、每一屏一个视觉焦点。

**调研来源**（2026-08-23，外部开发者对"AI 前端味"的约束总结）：

- [AI Slop Design — Vibe Code Kit](https://vibecodekit.dev/ai-slop-design)：六类特征全清单（颜色/卡片边框/字体/布局/暗色特效/图像动效）与 anti-slop 规则，本节分类框架的主要依据。
- [avoid-ai-design — GitHub](https://github.com/funboy322/avoid-ai-design)：Claude Code 审计 skill，本节 P0/P1/P2 分级与大量细项来源（icon-in-rounded-square、文案词表、默认 blue-600 等）。
- [Hallmark — dev.to](https://dev.to/rams901/hallmark-stop-ai-generated-ui-slop-in-one-command-in-2026-3p9n)：65 个二进制 slop 门禁 + 主题化生成（拒绝训练分布默认值）；"Inter 在训练数据中被过度代表"。
- [优设《AI 生成的网页总爱用蓝紫渐变？真相藏在5年前！》](https://www.uisdc.com/ai-design-bias)：蓝紫渐变成因考证（Tailwind 默认 indigo 污染训练语料；Adam Wathan 致歉）。
- [Beyond "Make it Beautiful": The Anti-Slop Framework — Medium](https://moelkholy1995.medium.com/beyond-make-it-beautiful-the-anti-slop-framework-for-ai-frontend-craftsmanship-c99bbee6c994)：紫蓝渐变背景 / 居中徽章+大标题+三卡 / 假仪表盘。
- 中文社区总结（腾讯云开发者 / 优网科技）：AI 前端"四大通病"——蓝紫渐变、滥用 emoji、圆角泛滥、布局雷同。

---

## 13. ZCode 化视觉规格（阶段六~九实现依据，v2.0 新增）

> **依据**：晚风提供的 ZCode 2026-08 版全量截图（工作区空态/会话态、设置中心各页、Composer @ 与 / 展开态、权限档位菜单、使用统计、自动化、插件市场、文件树、审查 diff、头像快捷菜单）逐张核对写成，下文"实测"均指该批截图；主布局另援引 Codex app 公开资料旁证（深色侧栏+浅色内容区+按项目分组 threads，与 ZCode 同构——所选方向即业界当前范式，可放心定死）。
> **口径**：本节全部落数值，禁止形容词；实现工单见 doc/02 §8 阶段六（6.1~6.8）；规格有疑义先提勘误再动手。与 §1~§11 冲突处**以本节为准**（逐处标明"取代"）。

### 13.0 界面三分类（设计方法总纲）

| 属性         | 判定标准                                                       | 设计方法                                     | 本产品对应界面                                                     |
| ------------ | -------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------ |
| **会话投影** | 渲染对象是事件流（applyEvent 加一种事件即多一种渲染）           | 不依赖参考截图，协议定了界面自然定了（§8 管形态） | 消息流、工具卡、审批卡、思考块、子代理行内卡、变更聚合条、StatusBar |
| **配置 CRUD** | 操作对象是配置实体与存量状态（事件流里不存在"插件已安装"这类事件） | 传统表单/列表页，逐字段定规格（13.D/13.F）   | 设置中心 15 页、插件市场、自动化、命令面板、文件树                  |
| **聚合投影** | 事件的聚合统计（usage / permission 决策）                       | 图表规格（13.G）                             | 使用统计、审计日志                                                  |

### 13.A 布局栅格（取代 §2 数值）

**三栏骨架**（会话态；空态无右栏）：

```
┌────────────┬──────────────────────────────────────┬─────────────┐
│ 左栏 264px │ 顶栏 44px：会话标题 · 项目 chip · 分支 chip · ⋯ │ 右栏 360px   │
│ (可折叠48) ├──────────────────────────────────────┤ tab: 审查 /  │
│ 新建任务   │        消息流（内容列 768px 居中）        │ 子代理运行 / │
│ 搜索       │        思考块 · 工具卡 · 审批卡 · 聚合条    │ 辅助对话 N /+│
│ 自动化     │                                      │ (可整体收起) │
│ 插件市场   │  ┌────────────────────────────────┐  │             │
│ ─────────  │  │ Composer 沉底（768px，见 13.E）  │  │             │
│ 项目分组 ▾ │  └────────────────────────────────┘  │             │
│ ▸ Spark    ├──────────────────────────────────────┴─────────────┤
│ ▸ GeoWork  │ StatusBar 24px 单行细条：●连接 · 模型 · 水位 · 用量 · 模式 │
└────────────┴────────────────────────────────────────────────────┘
```

**空态页**（垂直居中，取代 §7.1"紧凑引导块"的具体形态）：

```
┌────────────┬──────────────────────────────────────┐
│ 左栏 264px │                                      │
│            │        问候语（15px semibold）          │
│            │     ┌──────────────────────────┐      │
│            │     │ Composer 居中 560px（13.E） │      │
│            │     └──────────────────────────┘      │
│            │    [chip] [chip] [chip] [chip] 4 枚     │
└────────────┴──────────────────────────────────────┘
```

数值清单：

| 项                 | 值                                                                                   |
| ------------------ | ------------------------------------------------------------------------------------ |
| 左栏               | **264px**，可折叠至 48px 图标态；分组头高 28px（12px 字号）；会话项高 32px；行内边距 12px |
| 项目分组           | 会话按项目折叠分组（实测 ZCode"分组/项目"双模式）；**数据源=磁盘 cwd 目录结构（mungeDir 已按项目分目录存会话），纯前端点亮** |
| 顶栏               | 高 44px；标题 13px semibold 截断；项目/分支 chip 高 24px                                |
| 主区内容列         | **max-width 768px 居中**；两侧留白随窗口伸缩；只有消息流纵向滚动                        |
| 右栏               | 360px，tab 化（审查/子代理/辅助对话/＋），可整体收起；辅助对话=独立 Composer（13.E 同款） |
| StatusBar          | **单行细条 24px**：连接状态点+文案 · 当前模型 · 上下文水位（百分比，>80% 转 warn 色） · 本会话 token 用量 · 提交模式（now/steer/queue） |
| 最小窗口           | 1280×800 不变（§2）；三视口走查档 1280/1440/375（375 仅移动端预研，工单 6.8）            |

### 13.B 控件规格表（细化 §3）

| 控件       | 高度            | 圆角    | 字号         | 备注                              |
| ---------- | --------------- | ------- | ------------ | --------------------------------- |
| 按钮       | sm 28 / md 32 / lg 38 | 6px     | 13px         | 主按钮每屏至多一个（§3 不变）     |
| 输入框     | 38px            | 6px     | 13px         | 聚焦 ring 2px                     |
| 下拉选择   | 32px            | 6px     | 13px         | 触发器带 16px chevron             |
| 分段控件   | 28px            | 6px     | 12px         | 选中段 zinc-100 底（暗 zinc-800） |
| chip/徽章  | 24px            | full    | 11px         | 图标 12px + 文案                  |
| 卡片       | —               | 8~12px  | —            | 1px border，不用阴影（§3 不变）   |
| 弹层菜单   | 行高 32px       | 8px     | 13px         | 分组头 11px meta 色；一层 subtle shadow |
| 页面大标题 | —               | —       | 20px semibold | 仅设置中心/管理页页头             |
| 区块标题   | —               | —       | 14px semibold |                                   |
| 正文/次级/meta | —           | —       | 13 / 12 / 11 | §3 不变                           |

- 间距：**4 的倍数**；卡片内边距 12~16px；区块间 24px。
- 图标：16px 线性（1.5px stroke），lucide；状态点 8px 圆（§8 不变）。
- 三态色值（zinc 标尺）：hover = zinc-100（暗 zinc-800）；active = zinc-200（暗 zinc-700）；disabled = 前景 opacity 40% + `cursor: not-allowed`。
- **界面字号（外观设置）**：档位 12/13/14/16，**默认 13**（与 §3 密度一致；ZCode 实测默认 14px，Spark 取更紧凑一档，要对齐改默认即可）；只缩放字号，图标与布局尺寸不受影响（实测 ZCode 同款行为）。
- **代码字号（外观设置）**：默认 **12px**（实测 ZCode 12px；§3 的 mono 12.5px 由该设置接管）。

### 13.C 主题 token 表（亮色为 :root 默认——取代 §4"深色默认"）

- 三档：**浅色（默认）/ 深色 / 跟随系统**；手动选择持久化 localStorage；系统档监听 `prefers-color-scheme`。
- 亮色挂 `:root`，深色收 `.dark`；变量名与 Tailwind v4 `@theme` 对齐。
- 对比度标注为近似值（白底/黑底计算），**工单 6.1 落地时以工具复核**：正文 ≥4.5:1，大字与 UI 组件 ≥3:1。

| Token                        | light（:root 默认） | 对比度      | dark（.dark）   | 对比度      |
| ---------------------------- | ------------------- | ----------- | --------------- | ----------- |
| `--background`               | `#ffffff`           | —           | `#09090b`       | —           |
| `--foreground`               | `#18181b`           | ≈17.4:1 AAA | `#fafafa`       | ≈17.4:1 AAA |
| `--card` / `--popover`       | `#ffffff`           | —           | `#18181b`       | —           |
| `--primary`                  | `#18181b`           | —           | `#fafafa`       | —           |
| `--primary-foreground`       | `#fafafa`           | ≈17.4:1     | `#18181b`       | ≈17.4:1     |
| `--secondary` / `--accent`   | `#f4f4f5`           | —           | `#27272a`       | —           |
| `--muted`                    | `#f4f4f5`           | —           | `#27272a`       | —           |
| `--muted-foreground`         | `#71717a`           | ≈4.6:1 AA   | `#a1a1aa`       | ≈7.6:1 AA   |
| `--destructive`              | `#dc2626`           | ≈4.5:1 AA   | `#f87171`       | ≈5.9:1 AA   |
| `--border` / `--input`       | `#e4e4e7`           | —           | `#27272a`       | —           |
| `--ring`                     | `#a1a1aa`           | —           | `#52525b`       | —           |
| `--spark-accent`（运行/链接/焦点） | `#4f46e5`     | ≈6.3:1 AA   | `#818cf8`       | ≈5.3:1 AA   |
| `--spark-warn`（审批/警示）  | `#b45309`           | ≈4.7:1 AA   | `#fbbf24`       | ≈10:1 AA    |
| `--spark-ok`（成功）         | `#047857`           | ≈5.2:1 AA   | `#34d399`       | ≈9:1 AA     |
| `--spark-err`（错误/断线）   | `#b91c1c`           | ≈5.9:1 AA   | `#f87171`       | ≈5.9:1 AA   |
| 侧栏底 `--sidebar-bg`        | `#fafafa`           | —           | `#18181b`       | —           |
| 侧栏选中项                   | `#f4f4f5` + 1px `#e4e4e7` 边（实测 ZCode） | — | `#27272a` | — |

- 代码主题（外观设置，**浅深分开选**，实测 ZCode 同款）：浅色默认 **GitHub Light**、深色默认 **Minimal Dark**；DiffViewer/CodeBlock 接入该设置（工单 6.4）。
- 代码预览：外观设置底部**浅/深双栏并排实时预览**，当前生效主题带"当前生效"badge（实测）。
- 用户消息块：浅色下 zinc-100 级浅灰全宽块（实测），与 §3 转录式一致。

### 13.D 设置中心信息架构（三组 15 页，配置 CRUD）

- **入口**：左栏底部用户卡旁齿轮；另设头像快捷菜单（实测）：界面语言 › / 界面主题 › / 界面缩放 › ｜ 使用统计 ｜ 升级 ｜ 断开连接。
- **导航**：复用左栏 264px，三组——**基础设置**（常规/外观/模型设置/浏览器/电脑控制）、**Agent 能力**（记忆/子智能体/插件/MCP 服务器/技能/命令/钩子）、**数据与统计**（索引库/使用统计/引导）。取代 D3 早期"六分区"草案。
- **页面骨架**：内容列 max-width 768px 居中；页头=大标题 20px + 说明文案 12px meta 色；设置行=左侧"标题 13px + 说明 12px"、右侧控件，行高 56~64px，行间 1px border 分隔；相关行合入圆角 8px 分组卡。

**① 常规**（实测全量字段；"Spark 落点"标注实现归属）：

| 字段 | 形态 | 默认 | Spark 落点 |
| --- | --- | --- | --- |
| 界面语言 | 下拉 | 中文简体 | i18n 框架（平台缺口项），web/desktop 生效 |
| 继承系统终端 Profile | 开关 | 开 | desktop 特化（内置终端，P2） |
| 终端字体 | 文本+保存钮 | 留空自动探测 | desktop 特化 |
| 集成终端 Shell | 下拉 | 自动选择（Git Bash 优先，回退 cmd.exe，实测） | desktop 特化；引擎 bash 工具 Windows 同源 |
| 增强 Find 和 Grep | 开关 | 开 | 工具增强开关（7.x 评估） |
| HTTP 代理 | 文本+保存钮 | 空=直连 | **引擎出网代理**（缺口项）：模型/MCP/命令工具出口流量经此，不读系统环境变量；改后重启生效 |
| 不使用代理的地址 | 文本+保存钮 | 空 | 逗号分隔规则，匹配主机直连 |
| 自定义证书 | 文本+保存钮 | 空 | PEM 路径 → NODE_EXTRA_CA_CERTS 注入模型/MCP/命令工具 |
| Chrome 硬件加速 | 开关 | 开 | desktop 特化（白屏/闪退规避） |
| 接受预览版更新 | 开关 | 关 | 自更新通道 |
| 自动下载并安装更新 | 开关 | 开 | 自更新；任务运行时重启前要求确认（实测） |
| 任务通知 | 开关 | 开 | 通知：完成/失败/需确认 → 系统通知 |
| 通知声音 | 开关 | 开 | 通知 |
| 关闭窗口时隐藏到托盘 | 开关 | 开（仅 Windows，实测） | 托盘 |
| 保持电脑运行 | 开关 | 关 | 阻止空闲休眠（7.6 自动化前置） |
| 交互行为 | 下拉（队列 / 引导至下一轮工具调用后运行） | 队列 | **提交三模式 now/steer/queue 的默认档**（13.E） |
| 提问自动继续 | 开关 | 开 | Agent 提问 5 分钟未答自动继续（实测） |
| 完整保留模型 I/O | 开关 | 关 | 关闭自动压缩/限长/清理（与 Compaction 策略互斥） |
| 显示思考过程 | 开关 | 开 | 关时每轮仅展示第一次思考（13.H） |
| 显示待办 | 开关 | 关 | Todo 工具卡片显隐 |
| 分组探索工具 | 开关 | 开 | 连续读/搜索聚合为 Explore 分组（13.H） |
| 分组终端命令 | 开关 | 开 | 连续非只读 shell 聚合为 Terminal 分组 |
| 分组文件更改 | 开关 | 关 | 连续写聚合为 Changes 分组 |
| 自动归档旧任务 + 归档保留时长 | 开关 + 下拉 | 关 / 7 天 | 已完成/无未读/未置顶且超期 → 归档 |
| 数据存储路径 | 文本+选择文件夹+保存 | 用户主目录 | Spark 对应 `~/.spark/`；迁移=复制现有数据，路径后缀不可改（实测） |
| 引导 | 按钮"打开引导" | — | onboarding 重看/迁移导入 |
| 优化体验 | 开关 | 关 | 遥测；Spark 默认关=不收集 |

**② 外观**（实测全量字段）：界面主题（三档下拉，默认浅色）/ 界面字号（默认 13，见 13.B）/ 浅色代码主题（GitHub Light）/ 深色代码主题（Minimal Dark）/ 显示行号（开）/ 长行自动换行（开）/ 代码字号（12px）/ 代码预览（浅深双栏并排+当前生效 badge）。

**③ 模型设置**（实测全量字段；工单 6.5）：

- 左列表：两组——**内置** / **自定义供应商**（+添加供应商钮）；行=16px 图标+名称+状态点。
- 详情区：名称（行内改名铅笔）+ **已启用/禁用** badge 按钮 + 删除入口；Base URL 文本框；API 格式下拉（Chat Completions (/chat/completions) 等）；API Key（掩码+眼睛明文切换）。
- 模型列表：行=模型 ID + 上下文窗口 badge（200K 式）+ 编辑/预览/删除；+添加模型。
- 添加模型弹窗字段：模型 ID / 上下文窗口 / 最大输出 Token / 输入类型（文本□图片□视频，文本锁定勾选）/ 输出类型（文本锁定）。
- **连通测试决策**：实测 ZCode 无显式测试按钮（状态点=保存即测结果）；Spark 6.5 采用**保存即测+状态点**为主，供应商详情区另留显式"测试连接"按钮（工单备注单列）。

**④ 浏览器**：占位（未截图；对应 7.10 browser 工具族的数据清理/引擎设置，7.10 动手前补图）。**⑤ 电脑控制**：占位（未截图；开关+审批档位）。

**⑥ 记忆**：实测=单卡开关"工作区记忆"+说明（保存并复用长期上下文，新会话生效，开启可能增加模型调用和 Token 成本）；Spark 7.5 扩展为 开关+存储位置展示+条目管理列表。

**⑦ 子智能体**（实测）：作用域下拉+计数+搜索框；"已安装 N 项"自定义区，空态=虚线卡"没有找到子智能体——填写子智能体名称、工具和系统提示词，保存后返回列表"+新建钮；内置区行=名称+工具 badge（"全部工具"/"N 个工具"）+一句英文描述+权限模型下拉（"继承默认"）。

**⑧ 插件**：管理见 13.F.2 市场页+已安装列表。**⑨ MCP 服务器**（实测）：作用域下拉+计数+搜索；分组=**已安装**（用户自建）/ **按插件分组**（行说明"该 MCP 服务器内置在插件中，启用插件后会加载"）；行=图标+名称+传输·URL（http·https://… / stdio）+**三态连接状态点**（绿=已连接并可用 / 红=连接失败，请稍后重试 / 灰=插件内置未加载）+启停 toggle；新建=**表单｜JSON 双模切换**，表单字段=名称/作用域/类型（stdio（本地命令）/http）/超时 MS（30000）/命令/参数（空格分隔）/环境变量（可选，JSON 文本域）。

**⑩ 技能**：列表+启停（同插件载体）。**⑪ 命令**（实测空态）："尚未安装命令——新建命令，或从外部 Agent 导入已有命令"+新建；7.4 自定义命令管理页形态，"从外部 Agent 导入"记入 7.4 备选验收。**⑫ 钩子**（实测空态）："尚未安装钩子——新建钩子，以在任务生命周期事件中运行命令"+新建钩子；7.3 用户侧 hooks 管理页形态。

**⑬ 索引库**：占位（未截图；Spark 对应 index.db 会话索引，7.13 前补图）。**⑭ 使用统计**：见 13.G。**⑮ 引导**：重新打开引导弹窗入口。

### 13.E Composer 规格（会话投影+配置混合；工单 6.3）

- 容器：宽 min(100%, 768px)（空态 560px）；圆角 12px；1px border，聚焦 ring 2px；内边距 12px。
- 占位文案：空态="向 Spark 提问，使用 @ 添加上下文，使用 / 选择命令或能力"（实测 ZCode 同构）；turn 运行中="继续输入以排队后续修改"（实测——**运行中 Composer 不禁用，输入即排队**）。
- 底部工具条：左=[＋附件/上下文] [权限档位]；中=[模型选择器（供应商/模型级联下拉）] [推理档位]；右=[发送/停止 32px 圆形主钮]，运行中变停止 ■。
- **权限档位菜单**（实测 ZCode 四档；Spark 词汇对齐）：

| 档位 | 说明 | 视觉 |
| --- | --- | --- |
| 逐项确认（默认） | 改文件前先问我 | 图标 zinc |
| 自动编辑 | 自动编辑文件，其余照旧审批 | 图标 zinc |
| 计划模式 | 先编制并确认计划，再执行 | 图标 zinc |
| 完全访问 | 减少确认次数 | **图标转 warn 琥珀色作警示**（实测） |

  当前档右侧勾选；档位随会话记忆。
- 提交模式：now/steer/queue 分段控件（28px，见 13.B），默认档取常规页"交互行为"。
- 多行：1 行起步，自增至 **6 行上限（约 144px）**后内部滚动；Enter 发送 / Shift+Enter 换行（§5 不变）。
- 附件：＋菜单（上传文件/截图）；剪贴板图片粘贴 → 80px 缩略图预览行，可单删——对应 user.message `attachments` 字段（协议已预留，P1 工单）。
- 快捷 chips：仅空态，4 枚 24px 胶囊（图标+文案），点击即填入输入框。
- **@ 菜单**（实测）：触发=输入 @；分组=文件 / 技能；行=16px 图标+名称+meta（来源 · N 技能 · N 工具）；底部提示"输入内容以搜索文件或技能"；↑↓ 选择、Enter 确认、Esc 关闭。
- **/ 菜单**（实测）：触发=输入 /；分组=命令 / 技能；命令行=/名称+一句描述（内置基线=7.4：/compact /model /mcp /skills /usage /resume）+自定义命令；技能行=$名称+描述+来源 badge（用户/内置）——**技能用 $ 前缀与 / 命令区分**（实测）；底部提示"输入内容以搜索命令、技能或子智能体"。

### 13.F 管理页规格（配置 CRUD）

**13.F.1 命令面板**：唤起 Ctrl/Cmd+P（Ctrl/Cmd+K 保留为会话切换）；四 tab=全部/操作/任务/文件（实测）；最近任务区置顶；建议区行=图标+名称+说明+**快捷键右对齐 kbd 标注**；输入即过滤。现有 CommandPalette 按此对齐升级（6.3）。

**13.F.2 插件市场页**（实测）：顶部搜索框（占位"搜索插件…"）；"已安装 N"横排（图标+名称，点击进设置·插件页）；tab=公开/个人；五分类=生产力/开发者工具/实用工具/指南/模板；卡片网格 3 列（图标+名称+一句简介+安装钮/已安装态）；徽章=公开/个人/已安装；右侧详情栏（大图标+名称+描述+版本+权限清单+安装/卸载）。v1 市场壳=纯前端+静态清单。

**13.F.3 自动化页**（实测；7.6 UI 形态定稿）：页头=标题+一句说明（"创建定时任务，或排队在闲置算力空闲时后台执行。"式）；空态=虚线圆角卡（居中文案+创建定时任务[主钮]+创建闲时任务[次钮]）；"运行会话时保持电脑唤醒"开关条；模板两组各 3 列卡栅格——闲时任务模板（图标+名称+描述+最早可用时段）/定时任务模板（图标+名称+描述+执行时间人话如"每工作日 09:00"）；我的任务列表+运行历史（时间+状态+耗时）。

**13.F.4 文件树抽屉**（实测）：入口=左栏切换钮（会话列表↔文件树）；顶部"返回任务"链接+搜索过滤框；仓库头=仓库名+分支 chip+刷新钮；树行=16px 类型图标+名称，**缩进 16px/级**，文件夹 chevron；**实测注记：ZCode 不灰显 .agents/.cursor 等配置目录、也不过滤 node_modules——Spark 同形态交付，"忽略规则"做设置项（默认关）**；点击文件→右栏打开预览/diff。

### 13.G 聚合投影页规格

**使用统计**（实测全量；6.6/7.11 同口径）：

- 指标卡行：5 张等宽卡（数值 20px + 标签 12px）=累计 Token / 峰值 Token / 最长聊天时长 / 当前连续天数 / 最长连续天数。
- Token 活动热力图：GitHub 贡献图式（列=周、行=星期，单色 7 档透明度），月份轴在下，档位切换=每日/每周/累计。
- 时间范围段控：近 7 日 / 近 30 日。
- 每日 Token 趋势折线：按模型分线，图例=顶部色点+模型名，hover 显数值。
- 模型用量环形图：左环=各模型扇区+中心总额；右列表=色点+模型名+tokens+右对齐百分比；**份额过小归并"其他模型"**（实测 3.2% 归并）；右下刷新钮。
- 数据源=assistant.message `usage` 事件聚合（6.6 用量条同源；7.11 扩展费用列）。

**审计日志**（7.12）：转录式明细流（时间+主体+工具/资源+决策 allow/deny+规则来源），复用 §8 卡片密度；顶部过滤器（时间/决策/工具）。

### 13.H 会话域实测形态（补充 §8，全部会话投影）

- 思考块：摘要行"思考过程 · 持续了 N 秒"+chevron，默认折叠，流式中实时计时；常规页"显示思考过程"关闭时仅首轮展示。
- 工具分组卡（常规页三分组开关控制）：连续读/搜索 →"Explore · N 项"分组行；连续非只读 shell →"Terminal · N 个命令"；连续写 →"Changes"分组（默认关）。
- 变更聚合条：turn 尾部"N 个文件已更改 +X −Y"+撤销钮；展开=每文件行（类型图标+mono 路径+增删统计+审查/打开钮）；点审查→右栏**行级 diff**（面包屑+行号+增行绿底/删行红底+"查看源码"链接）——P2 审查模式的实物标尺。
- 子代理行内卡：图标+"子智能体 名称 · 一句任务"+运行态点；右栏 tab 化承载（审查/子代理运行/辅助对话 N/＋）。
- 进程 popover：顶栏触发，"进程 N/M"清单，分已完成/进行中/待处理三组，行=状态图标+一句话任务。
- 顶栏 chip：项目文件夹名+分支名（实测）——项目分组数据源的前端入口。

### 13.I §12 反 AI 味清单四端适用条款

| 端 | 生效范围 | 附加禁令与适配 | grep 落点 |
| --- | --- | --- | --- |
| web | §12 全量 + §12.8 | — | apps/web、components/ui |
| desktop（Electron 壳） | 同 web（同一渲染层） | 原生菜单/托盘/通知文案同守 §12.7 | 同 web |
| cli（Ink） | §12.1/12.3/12.6/12.7 | 无 CSS：禁 ANSI 渐变与 256 色滥用（默认前景+zinc 两档灰）；单行高密度；禁 emoji 装饰 | apps/cli（查 emoji 与硬编码 ANSI 色号） |
| mobile（RN+Expo） | §12.1/12.2/12.4/12.6/12.7 | 系统字体；13.C token 映射 RN 主题；禁多层阴影（elevation 单档） | apps/mobile |
| 小程序（Taro） | 同 mobile | 组件库白名单制；rpx 保持 4 的倍数 | apps/miniapp |

各端目录落地时把 §12.8 grep 表按上列适配进 CI；§12.7 文案词表四端同一份，不各自维护。

### 13.J 移动端规格（iOS/Android App，v2.2 新增；工单 9.2/9.3 实现依据）

> **依据**：晚风提供的 Qoder CN iOS v1.32.0 实拍截图 13 张（设置/用量/通知/清理缓存/账号与安全/扫码配对/远程控制引导/全部任务列表与筛选/主页空态/已归档抽屉），2026-08-26 逐张核对；下文"实测"均指该批截图。Spark 词汇映射逐页标注。**未截图页面（会话页有内容态/审批卡）不另立标尺**——会话域是投影（§13.0），形态从 §13.H 迁移。
> **框架**：Expo+React Native（ARCHITECTURE D20）；小程序端（Taro）形态同本节，尺寸单位换 rpx。

**J.0 设计基调（§13.C 的移动端适配）**

- 底色：浅灰 `#F7F7F7`（实测，zinc-100 级；暗色 `#09090b`）；内容全部承载于**白卡**——radius 16px、**无边框无阴影**，靠底色差分层（实测 Qoder 同款）；分组卡内部行间 0.5px hairline 分隔。
- 语义色沿用 §13.C token：toggle/选中/FAB/进度填充用 `--spark-accent`（**不引入 Qoder 的绿色**——accent 唯一纪律）；危险操作（断开连接/清理/撤销配对）用 `--spark-err` 红字；主 CTA 用 `--primary` 黑底白字胶囊。
- 字体：系统栈（iOS SF Pro / Android Roboto+思源黑体，§13.I mobile 行）；页头标题 17px semibold、行标题 16px、说明 13px meta、右侧值 15px meta。
- 图标：20px 线性（1.5px stroke）；chevron 16px meta 色。

**J.1 导航模式**

- 页头：居中标题 + 左浮动圆形钮 44px 白底（二级页=返回 chevron，根页=汉堡）+ 右浮动圆形动作钮（如刷新）；浮钮一层 subtle shadow（实测）。
- 根导航：**无 tab bar，单栈+抽屉**（实测 Qoder 同款）：首页=会话列表，左上汉堡开抽屉（会话/项目分组/设置/配对入口）。**落地收敛（工单 9.2 评审）：抽屉入口收敛为“全部会话/设置”两项——配对入口并入设置页（J.2.4/J.2.9），项目分组随 v2 项目域（V2-23）接入**。
- 底部安全区：所有固定底元素（Composer/CTA）避让 home indicator；键盘避让=Composer 随键盘上移（工单 9.3）。

**J.2 逐页规格**（括号=Qoder 对应页）

1. **首页/空态**（实测主页）：全屏留白（禁 hero/插画填充）；底部 Composer 胶囊——左右边距 16px、高 52、radius full、白底：左"+"圆钮 32px（附件/上下文占位）、占位文案"想让 Spark 做点啥？"13px meta、右发送黑圆钮 40px（turn 运行中变停止 ■）；左上汉堡圆钮。
2. **会话列表**（实测全部任务）：标题行"全部会话 ˅"（17 semibold+下拉 chevron）→ 筛选菜单（白卡 radius 12、行高 44、图标+文案、选中 ✓）：全部/按项目/已归档；时间分组（"今天/更早 ˅"分组头 13 meta，实测"更早 ˅"可展开）；行=状态点 8px（绿空闲/accent 运行/amber 待审批/灰完成，同 §8 状态点）+标题 16px 单行截断+右侧日期 13 meta；行高 52；右下 **FAB 56px accent 圆钮白+"**（新建会话，实测同位）。归档动作记 v2（列表筛选 v1 先行）。
3. **会话页**（实测：2026-08-26 补两张有内容态截图，本条为移动端消息密度标尺）：
   - 页头：返回圆钮 + 标题 17 semibold + **副标题行 13 meta**（图标+来源/项目名，实测"☁ 云端"位——Spark=项目名/cwd 目录名，§13.A 项目分组同源）+ 右"…"圆钮 → **会话菜单**（白卡 radius 16、行高 48、图标 20+文案 16、subtle shadow）：置顶（v2）/ 重命名 / 反馈（占位）/ hairline 分隔 / 归档（v2）/ **删除（红字，二次确认）**——删除/归档依赖 V2-23 后端（现无 DELETE 端点）。
   - 消息流形态（**移动端与 web §3 的唯一形态分歧，按实测定死**）：
     - **user 消息 = 右对齐浅灰胶囊**：zinc-100 底、radius 18、内边距 10/14、最大宽 80%、16px——**无头像**（§12.7 禁的是"大头像气泡聊天"，无头像不违反；assistant 永不做气泡）；
     - **assistant 消息 = 全宽纯文本**：17px、段落间空行、无底色无边框（实测）；
     - 每条 assistant 尾部**操作行**：复制 + 👍 + 👎 + hairline + "内容由 AI 生成"（13 meta）——v1 只做复制+标注（标注为生成式 AI 合规显式标识），👍👎 记 v2（需反馈存储）；
     - **居中时间戳分隔**：相邻消息间隔 >30min 插入居中"7月25日 18:30"式时间戳（13 meta，实测）；
     - 流式：新文字渐显（opacity 微动效，实测半透明渐入）；上滚/流式中右下**回到底部浮动圆钮** 44px 白底 ↓（同 web BackBottom）。
   - 工具卡单行折叠、思考块折叠、审批卡（白卡+warn 左边条+三键纵向全宽）、断线细条与错误人话文案：均按 §13.H 迁移，不变。
   - Composer 胶囊同 J.2.1，占位文案"描述你的任务…"（实测）。
4. **设置**（实测设置页）：头像行（头像圆 44+名 16 semibold+版本 badge[浅灰底 11px]+右侧扫码图标）→ 分组卡：①用量（>）②通知（>）/语言（值"中文"+上下 stepper，实测）/外观（值"跟随系统"+stepper→弹出菜单：跟随系统✓/浅色/深色，实测菜单形态）③清理缓存（>）④反馈（占位）/账号与安全（>）/关于 Spark（>）/检查更新（右侧版本号，实测 V1.32.0 位）⑤**断开连接**（红字，=撤销配对断开 server）。行高 56、hairline 分隔。
5. **用量**（实测套餐与用量）：白卡两组进度条——会话累计 token 与熔断阈值（7.7）：label 15px+进度条高 4 radius full（track zinc-200/填充 accent）+"已用 X / 阈值 Y"13 meta+右对齐"剩余 Z"；"查看详情"行（>）——V2-07 前移动端只读，提示到桌面端看图表；实测底部黑色"订阅"胶囊 CTA——Spark 无订阅，**该位改为"清零本机统计"或省略**（实现取舍记 PR）。
6. **通知**（实测通知页）：状态卡（手机插画图标+系统通知+说明+右"已开启 >"跳系统设置）+分组开关卡：**审批等待**（permission.asked 推送）/ **任务完成**（turn.completed 推送）/ 问答（占位禁用）/ 计划审查（占位）——iOS 式 toggle 51×31 选中 accent；映射 V2-05。
7. **清理缓存**（实测同名页）：白卡两行——应用缓存（"会话和消息在本机的缓存"，右字节数+红"清理"）/ 产物缓存（"已下载产物预览的磁盘缓存"）+独立白卡"全部清理"红字居中；右上圆刷新钮重算占用（实测）；本机缓存清理不属文件删除保护范围，但 UI 仍二次确认。
8. **账号与安全**（实测同名页）：白卡行"配对设备"+右侧值（已配对 N 台）>；独立白卡"断开并撤销配对"红字；顶部行显示 server 地址+连接状态（**Spark 无账号体系**——实测的"账号/注销账号"不映射，红线 §2.9）。
9. **配对流程**（实测扫码配对 sheet）：桌面/web 设置页出示 QR（内容 `spark://pair?host=<host>&port=<port>&code=<一次性短码>`）→ App 扫码 → 确认卡（server 地址+连接/取消）→ 换长效 token；**扫码为主、手输 6 位码兜底**（无相机/扫码失败）；sheet=白底 radius-top 16+拖拽把手+居中标题+右圆 X（实测形态）。→ ARCHITECTURE D24 补记。
10. **配对引导页**（实测 Remote-Control 页）：首次使用引导：虚线边框插画卡（radius 12，内为**线框**手机+电脑+虚线箭头，禁 3D 拟物）+步骤 bullet 列表（桌面开启非环回绑定→设置页出示 QR→本 App 扫码）+命令 chip（mono 13px、浅灰底 radius 6，如 `pnpm --filter @spark/server start --host 0.0.0.0`，实测绿色命令位改 accent 色）。
11. **已归档抽屉**（实测已归档）：底部 sheet（高 90%、把手、居中标题、右圆 X）；空态=居中两行式（粗标题 17+说明 13 meta，实测"暂无已归档任务"形态）；v1 占位。

**J.3 控件规格表（移动端）**

| 控件 | 规格 |
| --- | --- |
| 卡片 | radius 16、无边框无阴影、内边距 16、卡间距 12 |
| 分组行 | 高 56、hairline 分隔（缩进对齐文案） |
| 页头/行标题/说明/值 | 17 semibold / 16 / 13 meta / 15 meta |
| 浮动圆钮 | 44 白底 subtle shadow、图标 20 |
| FAB | 56 accent 圆钮、白"+"24、右下 16/24 |
| toggle | iOS 式 51×31、选中 accent |
| stepper | 当前值+上下 chevron 16 meta |
| 主 CTA | 全宽黑胶囊高 52 radius full 白字 16 |
| 危险操作 | 红字 16 居中独立白卡 |
| 菜单/sheet | 白底 radius 12/16、subtle shadow 一层、拖拽把手 36×5 zinc-300 |
| 进度条 | 高 4 radius full、track zinc-200、填充 accent |
| 代码 chip | mono 13、浅灰底 radius 6、内边距 4/8 |

**J.4 空态/加载/错误**：空态=居中两行式纯排版（实测，禁插画填充）；加载=骨架屏（卡片灰块），禁满屏转圈；错误=顶部细条+人话文案表（共享）+重试钮。

**J.5 反 AI 味移动端补充（§13.I mobile 行细化）**：插画仅限 J.2.10 虚线线框风格或纯排版；禁 3D 拟物/渐变/emoji/hero 大标题；动效仅微动效（按压缩放 0.97/120ms、sheet 弹出 240ms ease-out），禁页面转场编排。

---

_本文完（v2.4）。前端开发中与本文冲突的实现一律以本文为准；阶段六~九范围内 §13 与 §1~§11 冲突时以 §13 为准。需要突破规则时先改本文（附版本记录）再写代码。_
